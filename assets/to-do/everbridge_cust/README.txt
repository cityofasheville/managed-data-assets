Everbridge never worked from Windows so I rewrote this as a Nodejs script on an AWS Linux server. 
This still creates a daily file but it isn't loaded anywhere.

If Bedrock is rewritten to run multiple copies (unix/windows, different times of day, etc.) this 
might get used again.

